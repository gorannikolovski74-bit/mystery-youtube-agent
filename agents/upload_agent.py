"""upload agent — placeholder. Implemented in a later step (see HANDOFF).

Each agent exposes a `run()` entry point so run_pipeline.py can call it.
"""
from __future__ import annotations


def run(*args, **kwargs):
    raise NotImplementedError("upload_agent.run() is not implemented yet.")
